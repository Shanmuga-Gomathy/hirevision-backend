# Development Profile Configuration
# This profile disables email sending for development

# Disable email sending in development
spring.mail.host=localhost
spring.mail.port=1025
spring.mail.username=dev
spring.mail.password=dev

# Log email attempts instead of sending
logging.level.com.referralapp.email=DEBUG 